/**
 * Visible build stamp — bump on every user-facing ship so we can prove
 * the device is running the new bundle (Settings → About).
 */
export const ONA_BUILD_ID = "2026-08-09-shop-live-admin-v4-hooksfix";
export const ONA_BUILD_LABEL = "ONA Shop live · admin delivery v4 · hooks fix";
