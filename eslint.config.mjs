import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // The app deliberately fetches data in effects via async load() callbacks
      // (setState happens after await, never synchronously) and uses the
      // "latest callback via ref" pattern throughout. The React-Compiler-era
      // versions of these rules flag that established pattern app-wide
      // (124 + 5 pre-existing instances), so we keep them off.
      "react-hooks/set-state-in-effect": "off",
      "react-hooks/refs": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
