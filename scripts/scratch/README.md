# Scratch / probe scripts

One-off auth bisects, schema probes, and copy jobs. **Not product.**

Root `*.mjs` probes belong here so onboarding is not 30 mystery files.

These `*.mjs` / `*.sh` files are gitignored (`/scripts/scratch/*.mjs`). Keep them local.
