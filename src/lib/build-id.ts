/**
 * Visible build stamp — bump on every user-facing ship so we can prove
 * the device is running the new bundle (Settings → About).
 */
export const ONA_BUILD_ID = "2026-08-10-ship-status-api";
export const ONA_BUILD_LABEL =
  "Ship-status API · filters array fix · flat vehicles";
